const $=s=>document.querySelector(s),esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"})[c]);
function cards(s){const i=s.intent||{},e=s.entry||{},bp=s.processes?.bootstrap||{},fp=s.processes?.formal||{};const process=fp.alive?fp:bp;return `<div class="card"><small>自然语言请求</small><strong>${esc(s.request||"等待输入")}</strong></div><div class="card"><small>识别的软件</small><strong>${esc(i.display_name||i.app||"判断中")}</strong></div><div class="card"><small>入口状态</small><strong>${esc(e.status||"等待探索")}</strong></div><div class="card"><small>控制进程</small><strong>${process.stopped?"已暂停":process.alive?"运行中":"尚未运行"}</strong></div>`}
function bootstrap(s){const latest={};(s.progress||[]).forEach(x=>latest[x.step]=x);const order=["request","intent","observatory","entry","handoff"];const names={request:"收到自然语言",intent:"理解用户意图",observatory:"启动观测界面",entry:"发现本机入口",handoff:"交给正式 AXIS"};return order.map(k=>{const x=latest[k]||{status:"pending",message:"等待前一步"};return `<article class="step ${esc(x.status)}"><span class="state">${esc(x.status)}</span><h3>${names[k]}</h3><small>${esc(x.message)}</small></article>`}).join("")}
function phases(s){const rows=s.phases||[];if(!rows.length)return '<div class="empty">先导流程尚未把任务交给正式 AXIS。</div>';return rows.map(p=>`<article class="phase ${esc(p.status)}"><div><span class="state">${esc(p.id)} · ${esc(p.status)}</span><h3>${esc(p.name)}</h3></div><div class="children">${p.children.map(c=>`<div class="child ${esc(c.status)}"><i class="dot"></i><div><b>${esc(c.name)}</b><small>${esc(c.owner||"")} ${esc(c.detail||"")}</small></div></div>`).join("")}</div></article>`).join("")}
function batches(s){const b=s.batches||{},f=b.filtering||{},i=b.implementation||{};if(!Object.keys(b).length)return '<div class="empty">正式流程开始后，这里显示过滤批次和命令实现结果。</div>';return `<div class="card"><small>语义过滤批次</small><strong>${f.completed||0}/${f.total||0}</strong></div><div class="card"><small>计划实现命令</small><strong>${i.planned||0}</strong></div><div class="card"><small>验证通过</small><strong>${i.completed||0}</strong></div><div class="card"><small>失败已记录</small><strong>${i.failed||0}</strong></div>`}
async function refresh(){try{const r=await fetch('/api/snapshot',{cache:'no-store'}),s=await r.json();$('#connection').textContent='已连接 · '+new Date(s.now).toLocaleTimeString();$('#hero').innerHTML=cards(s);$('#bootstrap').innerHTML=bootstrap(s);$('#phases').innerHTML=phases(s);$('#batches').innerHTML=batches(s)}catch(e){$('#connection').textContent='连接失败'}}refresh();setInterval(refresh,2000);
