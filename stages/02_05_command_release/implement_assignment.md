
## Assigned batch

```json
{{BATCH_JSON}}
```

{{REPAIR_CONTEXT}}

This is an isolated candidate workspace. Modify only the assigned app inside
it. A merge Agent will later combine this candidate with the latest shared
engine. The Python controller owns merge scheduling, final build, and
verification.
